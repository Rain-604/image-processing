This project is change the RGB image into 4 gray level image with run length encode also with the eroor defusion then the run length decoding is decode all the txt file with the same patteren of encode from the python image 
